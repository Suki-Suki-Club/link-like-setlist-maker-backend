{
  "name": "link-like-setlist-maker-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/src/index.js",
    "build": "rm -rf dist && tsc",
    "typecheck": "prisma generate && tsc --noEmit",
    "test": "prisma generate && env TEST_DATABASE_URL=\"${TEST_DATABASE_URL:-}\" TEST_DIRECT_URL=\"${TEST_DIRECT_URL:-${TEST_DATABASE_URL:-}}\" tsx scripts/assert-test-database.ts && env DATABASE_URL=\"${TEST_DATABASE_URL:-}\" DIRECT_URL=\"${TEST_DIRECT_URL:-${TEST_DATABASE_URL:-}}\" prisma migrate reset --force --skip-generate && env TEST_DATABASE_URL=\"${TEST_DATABASE_URL:-}\" TEST_DIRECT_URL=\"${TEST_DIRECT_URL:-${TEST_DATABASE_URL:-}}\" DATABASE_URL=\"${TEST_DATABASE_URL:-}\" DIRECT_URL=\"${TEST_DIRECT_URL:-${TEST_DATABASE_URL:-}}\" vitest run",
    "dev:worker": "prisma generate && wrangler dev",
    "deploy": "prisma generate && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv cloudflare-env.d.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name init",
    "db:seed": "tsx prisma/seed.ts",
    "backfill:song-media": "tsx scripts/backfill-song-media.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend API for the Link Like setlist maker.",
  "dependencies": {
    "@hono/node-server": "^2.0.5",
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-openapi": "^1.4.0",
    "@prisma/adapter-pg": "^6.19.3",
    "@prisma/client": "^6.19.3",
    "@supabase/server": "^1.1.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.2",
    "hono": "^4.12.26",
    "kuroshiro": "^1.2.0",
    "kuroshiro-analyzer-kuromoji": "^1.1.0",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@types/pg": "^8.20.0",
    "prisma": "^6.19.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.106.0"
  }
}
